{
  "name": "tradingview-alerts-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "lint:server": "node --check index.js && node --check server/chartMath.js && node --check server/routes/chartRoutes.js && node --check server/routes/divergenceRoutes.js && node --check server/routes/healthRoutes.js && node --check server/services/divergenceService.js",
    "lint": "npm run lint:server && npm run typecheck",
    "test": "node --test \"test/**/*.test.js\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "lightweight-charts": "^4.2.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
